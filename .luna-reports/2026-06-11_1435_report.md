# 代码审查报告 · 2026-06-11 14:35

## 一、改动概述

共 433 行改动

## 二、后端审查

_未发现后端专项风险_

## 三、爆炸范围分析

### `FixCandidate` → `terminal_renderer.py:293`
- 风险: **high** | 置信度: low *(需人工确认)*
- 原因: 上下文标记 FixCandidate 为直接修改且风险高，但提供的 diff 未包含 terminal_renderer.py 中 FixCandidate 的实际改动，caller_contexts 也未展示真实调用片段，无法判断是否影响修复候选渲染、交互选择或测试断言。
- 建议: 人工核对 terminal_renderer.py 中 FixCandidate 的实际 diff，重点检查字段变更是否同步到渲染、排序、选择、测试数据构造和反序列化逻辑。

### `CallerContextConfig` → `config.py:73`
- 风险: **medium** | 置信度: high
- 原因: 新增配置类并接入 load_config，影响 caller_context 功能的开关、调用方数量和 snippet 行数上限。唯一调用方 snippet 仅显示测试导入 CallerContextConfig，未显示密集依赖具体字段，因此不提升为高风险。
- 建议: 补充配置加载测试，覆盖默认值、YAML 中 caller_context 覆盖值、未知字段过滤以及非法类型输入的行为。

### `Config` → `config.py:81`
- 风险: **medium** | 置信度: medium
- 原因: Config 新增 caller_context 字段，会改变配置对象结构并被后续上下文采集逻辑依赖。caller_contexts 中关于 Config 的大量调用 snippet 来自 .venv 第三方包，未真实引用本项目 config.Config，应降低外部爆炸范围判断；但本项目内部未展示完整调用片段，仍存在序列化、测试断言或构造 Config 时参数兼容性风险。
- 建议: 检查所有手动构造 Config(...) 的代码和测试是否受新增字段影响，并补充 load_config 在不存在 caller_context 配置时的默认值断言。

### `analyze` → `phases/blast_radius.py:104`
- 风险: **medium** | 置信度: high
- 原因: analyze 在 detail_level == "minimal" 时会裁剪 caller_contexts，只保留 symbol 和 total_callers。该行为会直接影响发送给模型的上下文质量；若下游提示词要求依据 snippet 判断依赖关系，minimal 模式下 snippet 被移除，可能导致模型只能基于数量而无法判断真实依赖。
- 建议: 在 minimal 模式中明确这是有意降级；或保留每个 symbol 的少量首个 caller snippet，避免与提示词中“优先依据 snippet 判断”的要求冲突。

### `ContextPack` → `phases/context_pack.py:9`
- 风险: **medium** | 置信度: high
- 原因: ContextPack 新增 caller_contexts 字段并在 to_dict 中序列化为 JSON。caller snippets 显示当前真实调用方多集中在 backend_context_pack/BackendContextPack，未直接展示对 phases.context_pack.ContextPack 新字段的密集依赖；但 to_dict 假设 caller_contexts 元素具备 symbol、total_count、callers 等属性，若传入 dict 或其他结构会在序列化时报错。
- 建议: 为 ContextPack.to_dict 增加单元测试，覆盖空 caller_contexts、SymbolCallers 对象、callers 为空、snippet 含多行文本等情况；必要时添加类型注解或运行时兼容 dict 输入。

### `ReviewReport` → `reporter.py:11`
- 风险: **medium** | 置信度: low
- 原因: ReviewReport 新增 caller_contexts 字段，用于终端渲染展示调用方上下文。caller_contexts 未提供 ReviewReport 的真实调用 snippet，无法确认所有构造、序列化、渲染路径是否兼容该新增字段；该项不是高风险，因此不强制人工复核。
- 建议: 检查 _make_report、报告导出、JSON/Markdown 渲染等路径是否需要包含或忽略 caller_contexts，并补充默认值兼容测试。

### `APIChangeConfig` → `config.py:68`
- 风险: **low** | 置信度: high
- 原因: 上下文标记为直接改动，但 diff 中 APIChangeConfig 本身仅作为新增 CallerContextConfig 的相邻位置出现，未看到字段或行为变化；caller_contexts 中也无外部调用证据。

### `BlastRadiusItem` → `phases/blast_radius.py:15`
- 风险: **low** | 置信度: high
- 原因: BlastRadiusItem 数据结构本身在 diff 中未显示字段变化，主要改动是 blast_radius 的系统提示词和 analyze 中对 context_pack.caller_contexts 的 minimal 模式裁剪。caller snippets 显示调用方主要按既有字段构造 BlastRadiusItem，未访问新增 caller_contexts，因此对该数据类直接风险较低。

### `CodeQualityItem` → `phases/code_quality.py:13`
- 风险: **low** | 置信度: high
- 原因: CodeQualityItem 数据类本身未在 diff 中显示字段变化，改动集中在系统提示词增加 caller_contexts 使用规则。caller snippets 显示调用方仍按既有字段构造 CodeQualityItem，未依赖新增上下文结构。

### `_SYSTEM_PROMPT` → `phases/code_quality.py:42`
- 风险: **low** | 置信度: high
- 原因: 仅提示词增加对 caller_contexts snippet 的使用约束，不改变本地解析结构。风险主要是模型输出策略变化，属于行为质量风险而非运行时兼容风险。
- 建议: 可增加基于固定 context_pack 的快照或契约测试，验证 prompt 仍要求只输出 JSON 数组且字段完整。

### `TestRenderReview` → `tests/test_terminal_renderer.py:226`
- 风险: **low** | 置信度: high
- 原因: 测试类新增 caller_contexts 渲染用例，验证普通模式展示 symbol/file、quiet 模式隐藏。属于测试覆盖扩展，对生产代码无直接运行时风险。
- 建议: 可补充 total_count 大于 callers 展示数量时的文案断言，以及 callers 为空时不崩溃的测试。


## 四、代码质量问题

### `config.py:130` — 新增 caller_context 配置解析缺少类型校验，配置项不是映射类型时会在加载阶段抛出异常。
- 类型: missing_error_handling | 风险: **medium** | 置信度: high
- 依据: if cc := raw.get("caller_context"):
    known = CallerContextConfig.__dataclass_fields__.keys()
    cfg.caller_context = CallerContextConfig(**{k: v for k, v in cc.items() if k in known})
- 建议: 在调用 cc.items() 前校验 isinstance(cc, dict)，否则给出明确错误或忽略非法配置。

### `phases/blast_radius.py:106` — minimal 模式下会移除 caller_contexts 中的 callers/snippet 信息，但系统提示仍要求模型依据 caller_contexts 中的真实调用片段判断风险，导致提示与实际输入不一致。
- 类型: logic_gap | 风险: **low** | 置信度: medium
- 依据: if detail_level == "minimal":
    _pack_dict["caller_contexts"] = [
        {"symbol": cc["symbol"], "total_callers": cc["total_callers"]}
        for cc in _pack_dict.get("caller_contexts", [])
    ]
- 建议: minimal 模式下可保留少量 callers/snippet，或在该模式下调整提示，明确只有调用数量没有代码片段。

### `phases/context_pack.py:43` — caller_contexts 序列化时直接假定每个元素都是 SymbolCallers 对象，缺少对空值或字典类型的兼容处理，可能导致上下文包生成失败。
- 类型: missing_error_handling | 风险: **medium** | 置信度: medium
- 依据: "caller_contexts": [
    {
        "symbol": sc.symbol,
        "total_callers": sc.total_count,
        "callers_shown": len(sc.callers),
        "callers": [
            {"file": c.file, "line": c.line, "language": c.language, "snippet": c.snippet}
            for c in sc.callers
        ],
    }
    for sc in self.caller_contexts
],
- 建议: 为 caller_contexts 明确类型，例如 list[SymbolCallers]；序列化前校验对象结构，或提供 from_dict/to_dict 统一转换方法，避免混入 dict 时 AttributeError。


## 五、关联测试用例

_未导入测试用例或无关联用例_

## 六、审查结论

> 由人工复审填写

---
*cr tool · 仅供参考*
